{
  "schemaVersion": "1.0.0",
  "description": "Firebase Firestore schema and security configuration for CRC Kids Church Hub account management and staff persistence",
  "entities": {
    "staff_account": {
      "description": "Team staff and volunteer user account entity",
      "fields": {
        "id": {
          "type": "string",
          "description": "Unique account identifier"
        },
        "name": {
          "type": "string",
          "description": "Full name of staff or volunteer member"
        },
        "email": {
          "type": "string",
          "description": "User email address"
        },
        "role": {
          "type": "string",
          "description": "Functional role: director, admin, tech, presenter, comms"
        },
        "roleTitle": {
          "type": "string",
          "description": "Display title for the staff member"
        },
        "assignedClassId": {
          "type": "string",
          "description": "Assigned class hub: jy, tb, kb, la-orange, la-yellow, all"
        },
        "phone": {
          "type": "string",
          "description": "South African mobile number"
        },
        "whatsapp": {
          "type": "string",
          "description": "International WhatsApp contact format"
        },
        "avatarColor": {
          "type": "string",
          "description": "Tailwind color gradient for the avatar"
        },
        "isClassAdmin": {
          "type": "boolean",
          "description": "Whether the user holds Class Admin administrative rights"
        },
        "pin": {
          "type": "string",
          "description": "4-digit quick switch and security authorization PIN"
        },
        "isAdminPromotedBy": {
          "type": "string",
          "description": "Director name who promoted this user to Class Admin"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when account was registered"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when account was last updated"
        }
      }
    }
  },
  "firestore": {
    "collections": {
      "staff_accounts": {
        "entity": "staff_account",
        "description": "Collection of staff and volunteer accounts across all classes"
      }
    },
    "securityRulesSummary": "Allows read and write access to staff_accounts for church team roster operations and account synchronization."
  }
}
